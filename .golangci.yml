version: '2'
run:
  allow-parallel-runners: true
linters:
  default: all
  settings:
    cyclop:
      max-complexity: 100
    funlen:
      lines: 600
      statements: 400
  exclusions:
    warn-unused: true
    rules:
      - path: .
        linters:
          - depguard
          - err113
          - exhaustruct
          - gochecknoglobals
          - gochecknoinits
          - goconst
          - gomodguard
          - gosec
          - ireturn
          - lll
          - mnd
          - noctx
          - revive
          - varnamelen
          - wrapcheck
          - wsl
      - path: .
        source: '^\s*defer\s+'
        linters:
          - errcheck
issues:
  max-issues-per-linter: 0
  max-same-issues: 0
  fix: true
